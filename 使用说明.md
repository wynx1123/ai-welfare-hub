# 使用说明

一套「数据驱动」的公益站聚合站：**加站只改一个 JSON**，其余（校验、探活、页面生成、链接体检、自动发布）全部由脚本 + GitHub Actions 完成。

## 快速开始

```bash
npm run all        # 校验 → 抓实时快照 → 生成页面 → 链接健康检查（一条龙）
npm run validate   # 只校验 sites.json
npm run refresh    # 只抓实时数据（在线状态/延迟/模型/签到开关）→ data/live.json
npm run build      # 只生成 README.md + docs/index.html
npm run check      # 只做链接健康检查
npm test           # 离线单测（不联网）
```

本地预览页面：`docs/index.html` 直接双击打开即可（纯静态，无依赖）。

## 如何添加你的站点（核心工作流）

编辑 `data/sites.json`，往 `sites` 数组追加一个对象：

```jsonc
{
  "id": "my-site",                          // 唯一 id：小写字母/数字/连字符
  "name": "站点名",
  "subtitle": "一句话定位（显示在卡片标题下方）",
  "recommended": true,                      // 可选：推荐站会高亮描边
  "credits": {                              // 额度口径（美元）；不确定就填 "approx": true
    "signup": 100,                          // 注册送
    "invite": 50,                           // 邀请加成（走本页邀请链接才有）
    "dailyCheckin": 25,                     // 每日签到
    "approx": false                         // true 时页面显示 ~$xx（约数）
  },
  "signupUrl": "https://example.com/register",
  "homeUrl": "https://example.com",
  "docsUrl": "https://example.com/docs",    // 可选
  "statusApi": "https://example.com/api/status",   // 可选：New API 系面板填这个，能自动抓到版本/签到开关/登录方式/公告
  "pricingApi": "https://example.com/api/pricing", // 可选：能自动抓到模型清单
  "mirrors": [                              // 可选：备用域名（大陆网络主域被墙时自动兜底探活）
    { "label": "大陆备用", "homeUrl": "https://m.example.com", "signupUrl": "https://m.example.com/register" }
  ],
  "tags": ["公益站", "免费额度", "Claude Code"],
  "highlights": ["卖点一", "卖点二"],        // 卡片上显示 2-3 条
  "endpoints": {                            // 可选：接入地址（展开"接入与详情"可见）
    "anthropic": "https://example.com",
    "openai": "https://example.com/v1"
  },
  "register": { "methods": ["GitHub OAuth", "邮箱注册"] },
  "caveats": ["注意事项，如封号红线/语言限制"],
  "community": ["Discord: https://..."]
}
```

最少只需要 `id / name / signupUrl / homeUrl` 四个字段，其余都能省。加完跑 `npm run all` 本地确认，提交即可。

### 积分制站点

`credits` 里填 `"unit": "point"` 即不折算美元，页面上按积分展示：

```json
"credits": { "invite": 600, "unit": "point" }
```

### 每日额度池型站点（如 RawChat）

```json
"credits": { "dailyQuota": 50 }
```

## 批量加站技巧

你手上站点多的话，不用一个个手写 JSON：

1. **同面板体系批量复制**：New API 系站点结构几乎一样，复制一条改 `id/name/域名/额度` 四处即可；`statusApi`/`pricingApi` 填 `<域名>/api/status` 和 `<域名>/api/pricing` 就能自动抓版本、模型、签到开关。
2. **先用最小字段跑通**：全部只填 `id/name/signupUrl/homeUrl`，`npm run all` 一次，看哪些站能被自动探活识别（`data/live.json` 里有 systemName 的就是 New API 系），再回头补 `statusApi`。
3. **校验会兜底**：`validate.mjs` 会拦住重复 id、非法 URL（非 http/https、内网/环回地址）、缺失必填字段，改到绿了再提交。

## 部署到 GitHub Pages（自动更新）

1. 建仓库推上去（`git init && git add -A && git commit -m init && git push`）
2. 仓库 Settings → Pages → Source 选 **GitHub Actions**
3. 把 `data/sites.json` 的 `meta.repoUrl` 填成你的仓库地址，`pagesUrl` 填成 Pages 地址
4. 完成。`.github/workflows/refresh.yml` 每 6 小时自动：校验 → 单测 → 抓实时数据 → 重建页面 → 链接体检 → 提交数据 → 发布

CI 里数据抓取失败时会**沿用上一次快照**（live.json 的 `staleFields` 会记录哪些字段是旧的），页面不会被刷空。

## 工作流细节

| 环节 | 行为 |
|---|---|
| 探活 | 主域名不通自动试镜像域名；6 站并发，单站 15s 超时 |
| 健康检查分三级 | ✓ 可达 / ~ 403 防护拦截（浏览器能开，不算死链）/ ⤷ 主域不通但镜像可达；只有真死链才让 CI 红 |
| 安全层 | 所有出站请求走 `fetchGuard`：仅 http/https、拒绝 localhost/环回/私网/保留地址、DNS 解析后二次校验（防 rebinding 到内网）|
| 页面 | 纯静态原生 JS：搜索框（站名/标签/卖点全文匹配）、标签筛选、按额度/名称/延迟排序、深色模式自适应 |

## 已知坑

- **403 ≠ 死链**：部分站（如 TaBiAI、GoRouter）对无头请求回 Cloudflare 403，浏览器正常。check 已按防护拦截处理，不报错。
- **主域被墙**：如 AgentRouter 主域大陆部分网络不通，务必维护 `mirrors` 字段，探活和健康检查都会自动走镜像。
- **改动 `scripts/` 后**：本地跑 `npm test` 确认 8 个用例还绿着再提交。
